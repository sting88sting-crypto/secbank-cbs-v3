# SecBank CBS V2 Environment Configuration
# SecBank CBS V2 环境配置
# Copy this file to .env and modify values / 复制此文件为.env并修改值

# ==========================================
# Database Configuration / 数据库配置
# ==========================================
POSTGRES_DB=secbank_cbs
POSTGRES_USER=secbank
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_PORT=5432

# ==========================================
# Backend Configuration / 后端配置
# ==========================================
SPRING_PROFILES_ACTIVE=prod
BACKEND_PORT=8080

# JWT Configuration / JWT配置
# Generate a secure 256-bit key for production / 生产环境请生成安全的256位密钥
# You can use: openssl rand -base64 32
JWT_SECRET=your-256-bit-secret-key-here-change-in-production
JWT_EXPIRATION=86400000

# ==========================================
# Frontend Configuration / 前端配置
# ==========================================
FRONTEND_PORT=80
